name: spendsense
description: A smart expense tracker

version: 1.0.4+4

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  provider: ^6.1.1
  http: ^1.1.0
  flutter_sms_inbox: ^1.0.3
  fl_chart: ^0.66.0
  intl: ^0.19.0
  google_fonts: ^6.1.0
  permission_handler: ^11.3.0

  firebase_core: ^3.6.0
  firebase_auth: ^5.3.0
  google_sign_in: ^6.2.1
  firebase_remote_config: ^5.0.0

  shared_preferences: ^2.2.3

flutter:
  uses-material-design: true

  assets:
    - shorebird.yaml